import React from 'react';
import ReactSalute from '../lib';

const Examples = () => {
  return (
    <section>
      <ReactSalute />
      <ReactSalute name="Fedor" />
    </section>
  );
};

export default Examples;
